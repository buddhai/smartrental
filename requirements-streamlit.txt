# Streamlit 버전 (기존 Flask/HTML과 별도)
# 설치: pip install -r requirements-streamlit.txt
# 실행: streamlit run streamlit_app.py

streamlit>=1.33.0
openpyxl>=3.1.0

# 기존 HTML+Flask 버전 (선택)
# flask
# flask-cors
